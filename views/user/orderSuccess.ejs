<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Order Success | TickerShop</title>

  <!-- Bootstrap & FontAwesome -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

  <style>
    body {
      margin: 0;
      padding: 0;
      min-height: 100vh;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.4)),
        url('https://images.unsplash.com/photo-1606813908967-910d7ae4cd8b') no-repeat center center/cover;
      display: flex;
      justify-content: center;
      align-items: center;
      overflow-x: hidden;
    }

    .success-card {
      background-color: #ffffffee;
      border-radius: 16px;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
      padding: 40px 30px;
      max-width: 600px;
      width: 100%;
      text-align: center;
      animation: fadeInUp 0.8s ease-in-out both;
      position: relative;
      z-index: 2;
    }

    @keyframes fadeInUp {
      0% {
        opacity: 0;
        transform: translateY(40px);
      }

      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .confetti {
      position: absolute;
      width: 100%;
      height: 100%;
      overflow: hidden;
      pointer-events: none;
      z-index: 1;
    }

    .confetti span {
      position: absolute;
      width: 10px;
      height: 10px;
      background: hsl(var(--hue), 70%, 60%);
      top: -10px;
      animation: confetti-fall 3s linear infinite;
    }

    @keyframes confetti-fall {
      0% {
        transform: translateY(0) rotate(0);
      }

      100% {
        transform: translateY(100vh) rotate(360deg);
      }
    }

    .success-icon {
      font-size: 90px;
      color: #28a745;
      margin-bottom: 20px;
    }

    .success-title {
      font-size: 30px;
      font-weight: 700;
      color: #222;
    }

    .success-text {
      font-size: 16px;
      color: #444;
      margin: 10px 0 25px;
    }

    .summary-box {
      background-color: #f8f9fa;
      border: 1px dashed #ced4da;
      border-radius: 10px;
      padding: 15px;
      text-align: left;
      margin-bottom: 25px;
    }

    .summary-box h6 {
      font-weight: 600;
      color: #444;
    }

    .summary-box p {
      font-size: 15px;
      color: #333;
      margin-bottom: 4px;
    }

    .btn-group {
      display: flex;
      flex-wrap: wrap;
      gap: 15px;
      justify-content: center;
    }

    .btn-custom {
      padding: 12px 24px;
      font-size: 16px;
      border-radius: 8px;
      font-weight: 600;
      border: none;
      transition: background-color 0.3s ease;
    }

    .btn-shop {
      background-color: #198754;
      color: white;
    }

    .btn-shop:hover {
      background-color: #157347;
    }

    .btn-view {
      background-color: #0d6efd;
      color: white;
    }

    .btn-view:hover {
      background-color: #0b5ed7;
    }

    @media (max-width: 576px) {
      .btn-custom {
        width: 100%;
        text-align: center;
      }
    }
  </style>
</head>

<body>

  <div class="confetti">
    <!-- 25 pieces of confetti -->
    <script>
      for (let i = 0; i < 25; i++) {
        const span = document.createElement('span');
        span.style.left = `${Math.random() * 100}%`;
        span.style.setProperty('--hue', Math.floor(Math.random() * 360));
        span.style.animationDelay = `${Math.random() * 2}s`;
        document.querySelector('.confetti').appendChild(span);
      }
    </script>
  </div>

  <div class="success-card">
    <i class="fas fa-check-circle success-icon"></i>
    <div class="success-title">Order Placed Successfully!</div>


    <div class="summary-box">
      <div class="success-text">
        Thank you for shopping with <strong>TickerShop</strong>! 🎉<br>
        We’re excited to get your order to you soon.
      </div>
      <p><strong>What's next?</strong></p>
      <ul class="ps-3">
        <li>Your order is being packed with care</li>
        <li>We hope to see you again soon!</li>
      </ul>
      <p class="mt-3"><em>Happy Shopping! 🛒</em></p>
    </div>
    <div class="btn-group">
      <a href="/shop" class="btn btn-custom btn-shop">Continue Shopping</a>
      <a href="/user/order" class="btn btn-custom btn-view">View Order</a>
    </div>
  </div>

</body>

</html>